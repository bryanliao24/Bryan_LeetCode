“单独元素的左边：pair 的第一个位置是偶；右边：pair 的第一个位置是奇。”

class Solution:
    def singleNonDuplicate(self, nums: List[int]) -> int:
        left, right = 0, len(nums) - 1
        while left < right:
            mid = (left + right) // 2
            if mid % 2 == 1:          # 让 mid 落在“对的第一个位置”
                mid -= 1
            if nums[mid] == nums[mid + 1]:
                left = mid + 2         # 这对配对正常，答案在右侧
            else:
                right = mid            # 这对不正常（错位），答案在左侧或即为 mid
        return nums[left]
