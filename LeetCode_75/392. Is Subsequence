bool isSubsequence(char * s, char * t){
    int index=0;
    int lent=strlen(t);
    int lens=strlen(s);
    int i=0;
    for(i=0;i<lent;i++){
        if(s[index]==t[i]){
            index++;
        }
    }
    if(index==lens){
        return true;
    }
    return false;
}
